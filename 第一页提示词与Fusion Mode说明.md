# 第一页提示词与 Fusion Mode 说明

> 更新日期：2026-07-30  
> 适用范围：AI Photo Editor 第一页“修图与融合”工具  
> 目的：记录 AI 优化提示词、Fusion Mode 预置提示词、服务端隐藏提示词以及图片处理流程，方便以后人工核对。

## 一、先看结论

第一页不同模式的实际处理并不完全相同：

- **Custom**：原生 Gemini API 流程。只发送原始图片、可选参考图以及输入框中当前可见的提示词；没有隐藏出图提示词。
- **AI 优化提示词**：只有用户主动点击按钮才会运行。改写结果会完整显示在输入框中，可以修改或恢复原文，不会在出图时暗中运行。
- **Remove Watermark**：固定使用一句去水印指令，但仍会经过一层通用修图提示词包装。
- **其他 Fusion Mode**：仍然使用双图角色划分、商业摄影、产品保真、光影融合、接触阴影等服务端隐藏提示词。
- **非 Custom 图片处理**：部分情况下还会压缩图片、补白，或者先把图片转成文字描述再重新生成。
- **第 2–5 页工具**：有各自独立的 HTML 和调用流程，没有随第一页一起修改。

因此：

> “Custom 没有隐藏提示词”是正确的；  
> “整个第一页所有模式都没有隐藏提示词”是不正确的。

---

## 二、AI 优化提示词

### 2.1 使用的文字模型

```text
gemini-3.5-flash-lite
```

只有点击：

```text
✨ AI 优化提示词
```

才会调用该文字模型。

它不会在生成图片时自动运行，也不会在用户看不见的地方修改提示词。

处理完成后：

1. 优化结果写回页面输入框。
2. 用户可以继续修改。
3. 用户可以点击“恢复原文”。
4. Custom 出图时只发送输入框里当前可见的最终内容。

### 2.2 当前系统提示词

```text
你是一个忠实、受控的电商图片提示词编辑器。你的任务是在绝不改变原意的前提下，整理用户原句，并且只对用户明确提出的要求维度做必要的执行性细化，使所选图像模型更容易准确执行。

必须遵守：
1. 用户原句是唯一事实来源。先识别用户明确提出了哪些维度，例如：任务目标、产品、文字内容、文字语言、文字排版、位置、颜色、数量、比例、场景、构图、风格、光线或需要保持不变的内容。
2. 只能整理或细化用户已经明确提出的维度；没有提出的维度不得主动补充。原句已经明确时允许原样返回，绝不能为了体现“优化”而增加长度。
3. 对用户已经提出但表达较笼统的维度，可以补充少量、不改变方向的执行性描述。例如“文字使用广告排版”可以细化为“主标题醒目、正文简短、字号层级清楚、对齐整齐、留白合理”，但这些描述只能约束文字排版。
4. 必须逐项保留原句中每个要求的作用对象、主语、修饰对象、范围和限定关系，不得扩大、缩小、转移或偷换。
5. 严格区分“文字内容”“文字语言”“文字排版”和“整体画面风格”：
   - 文字内容：用户提供的具体文字必须原样保留；除非用户明确要求，否则不得改写或翻译。
   - 文字语言：只规定最终图片中文字使用的语言，不得改成界面语言或画面风格。
   - 文字排版：只能细化标题层级、字号、字重、对齐、行距、位置、颜色、留白和可读性，不得扩大为整张画面的构图或风格。
   - 整体画面风格：只有用户明确要求广告图、海报、极简、奢华等画面风格时，才能细化场景、构图、光线和视觉氛围。
6. 涉及数量、位置、颜色、比例，以及“只修改”“不要”“保持不变”等硬性要求时，必须保留原来的对象和约束。
7. 不擅自增加商品、数量、品牌、具体文案、道具、人物、场景、风格、构图、教程、步骤、规格、价格、卖点或使用方法。用户要求文字排版但没有提供具体文案时，可以要求文字简短、层级清楚，并防止虚构价格、规格、品牌和卖点，但不得代写这些内容。
8. 不主动提及上传图片的数量，不推断多张图片之间的关系，不要求所有图片内容全部出现；除非用户原句明确提出。
9. 产品编辑时，可以在不冲突的情况下补充一句“保持产品外观和细节准确”。如果用户明确要求修改产品的某项属性，不得用保真要求阻止该修改。
10. 不添加“世界顶级、杰作、8K、商业摄影大师”等空洞词语，不堆砌负面提示词。
11. 输出应尽可能短，通常为1至3句，不设最低字数。只输出优化后的提示词正文，不要标题、解释、引号、Markdown或JSON。

语义边界示例：
原句：做一张产品使用示意图，用英文，文字使用广告排版
正确：制作一张产品使用示意图。所有文字使用英文；文字采用广告式排版，主标题醒目，正文简短，字号层级清楚，对齐整齐，留白合理。不要虚构未提供的价格、规格、品牌或卖点；保持产品外观和细节准确。
错误：画面采用英文界面与专业的广告排版设计，通过视觉引导展示产品的实际使用方法。
错误原因：把“文字使用广告排版”扩大成了“整个画面采用广告排版”，并擅自增加了视觉引导和使用方法。
```

### 2.3 针对不同图片模型的适配原则

AI 优化提示词时还会收到当前选择的图片模型名称和对应原则：

| 图片模型               | 优化原则                                        |
| ------------------ | ------------------------------------------- |
| Nano Banana        | 使用短而直接的自然语言，不增加原文没有提出的多阶段任务。                |
| Nano Banana 2      | 使用简洁、明确的自然语言，准确保留用户要求的对象、范围和限定关系。           |
| Nano Banana 2 Lite | 保持单一、明确的任务表达，不增加复杂要求，不把原句扩展成多阶段任务。          |
| Nano Banana Pro    | 可以准确表达复杂要求，但除非原文明确提出，否则不增加构图、风格、材质、文字或光影要求。 |

这些内容只帮助文字模型整理提示词。

最终图片模型收到的是输入框中可见的优化结果，不会收到这张表或 AI 优化器的系统提示词。

---

## 三、Fusion Mode 输入框预置提示词

选择 Fusion Mode 后，页面会把对应的预置内容写进提示词输入框。用户可以继续手动修改。

### 3.1 Custom

```text
空
```

Custom 不会覆盖用户已经输入的文字。

### 3.2 Remove Watermark

```text
去除右下角的gemini图标
```

### 3.3 Background Fusion

```text
请按参考图（图二）的背景场景与物理光影，将产品主图（图一）无缝融合，并在产品底部渲染真实物理接触阴影。
```

### 3.4 Style & Lighting Reference

```text
请参考第二张图的光影氛围与美学色调，将产品主图置于其中，协调整体光照与环境反射。
```

### 3.5 Replace Product

```text
请将参考场景（图二）中原有的旧物体移除，替换为图一中的主产品，严格匹配透视角度与阴影。
```

### 3.6 Add Logo / Watermark

```text
请将图二中的 LOGO/水印提取并自然叠加到产品主图之上，保持边缘清晰和适当透明度。
```

### 3.7 Replace Person

```text
请保持图一中人物的动作和服装不变，将其面容与头部特征替换为图二中的目标人物，做到光影无缝融合。
```

---

## 四、Fusion Mode 服务端隐藏提示词

除 Custom 外，其他双图 Fusion 场景不仅会发送输入框里的文字，还会在服务端加入图片角色、模式任务和统一商业摄影要求。

### 4.1 图片角色定义

单张主图时：

```text
IMAGE 1 = Main Product / Subject
最后一张图片 = Reference Blueprint
```

多张主图时：

```text
IMAGES 1–N = Complete Product Series & Kit
最后一张图片 = Reference Blueprint
```

多主图规则会把前面的图片视为完整产品系列和权威产品素材。

### 4.2 Background Fusion 隐藏任务

```text
PRIMARY TASK: Background Fusion & Scene Synthesis

- 主图包含必须保留的核心产品。
- 强调保留形状、纹理、品牌、文字和材质反射。
- 最后一张图是背景场景蓝图。
- 提取参考图的环境光、建筑风格、色彩和构图。
- 把主产品放入参考场景。
- 匹配主光方向和色温。
- 增加接触阴影、环境遮蔽和地面反射，避免贴纸感。
```

### 4.3 Style & Lighting Reference 隐藏任务

```text
PRIMARY TASK: Style, Atmosphere & Lighting Reference

- 主图是主体，保留主体结构和身份。
- 参考图提供氛围、光线、美学风格和色彩分级。
- 把参考图的环境光和美学氛围融合进主图。
- 保证阴影自然、颜色协调。
```

### 4.4 Replace Product 隐藏任务

```text
PRIMARY TASK: Product Replacement in Reference Scene

- 参考图是场景蓝图。
- 删除参考图中原来的产品。
- 替换为主图中的真实产品。
- 保持参考场景的光线、透视和景深。
- 为新产品生成真实阴影。
```

### 4.5 Add Logo / Watermark 隐藏任务

```text
PRIMARY TASK: Logo / Watermark Superimposition

- 主图是基础照片。
- 参考图是需要加入的 Logo 或图形。
- 把 Logo 自然叠加到主图。
- 保持透明度和边缘清晰。
```

### 4.6 Replace Person 隐藏任务

```text
PRIMARY TASK: Person Identity Replacement

- 主图是基础照片。
- 参考图提供目标人物身份。
- 替换人物身份。
- 匹配面部特征、肤色反射、光线角度以及头部和脖颈边界阴影。
```

### 4.7 双图 Fusion 统一结尾

各场景任务之后还会统一追加：

```text
Generate a single, flawless, commercial-grade photograph that executes the Primary Task and Custom Request.
Ensure zero sticker effect by strictly enforcing physical contact shadows and environmental light harmonization while including all uploaded product assets.
```

中文意思：

```text
生成一张无瑕的商业级照片，同时执行预置任务和用户要求。
通过接触阴影和环境光协调避免贴纸感，并纳入所有上传的产品素材。
```

### 4.8 通用修图外层包装

上面完整的 Fusion 提示词还会再被放进下面这层通用提示词：

```text
You are an expert AI photo editor. Edit the provided image according to the following instructions.

# EDIT INSTRUCTIONS:
[完整 Fusion 提示词]

# EDITING GUIDELINES:
1. Carefully perform the visual modification requested.
2. Ensure that any edited regions blend smoothly, naturally, and seamlessly with the surrounding colors, lighting, and textures.
3. Keep the rest of the image intact with high fidelity to the original.
```

所以其他双图 Fusion 场景目前属于“双层隐藏包装”：

```text
通用修图包装
└── Fusion 图片角色与商业摄影包装
    ├── 当前场景专属任务
    ├── 用户输入框提示词
    └── 商业级照片、接触阴影、避免贴纸感等统一要求
```

---

## 五、Custom 当前实际发送内容

Custom 的发送顺序是：

```text
原始主图 1
原始主图 2
……
参考图片（如果上传）
输入框中当前可见的提示词
```

Custom 当前没有：

- 通用修图提示词包装。
- 商业摄影大师提示词。
- Fusion 图片角色说明。
- 强制所有图片必须出现在结果中。
- 强制把多图识别为完整产品系列。
- 先让文字模型描述图片。
- 本地压缩。
- JPEG 转换。
- 白边补齐。
- 高保真模式隐藏提示词。
- 自动运行 AI 优化提示词。

如果用户点击过“AI 优化提示词”，发送的是已经显示在输入框中的优化结果。

如果用户没有点击，发送的是用户自己输入的原文。

### 5.1 Custom 宽高比控制

选择固定宽高比时，Custom 只增加 Gemini API 的图片配置，例如：

```json
{
  "imageConfig": {
    "aspectRatio": "16:9"
  }
}
```

选择“保持原始比例”时，不发送该宽高比配置。

这个属于 API 参数，不属于隐藏提示词。

---

## 六、Remove Watermark 当前流程

Remove Watermark 会：

1. 只使用第一张主图。
2. 不使用参考图。
3. 固定使用原始比例。
4. 关闭高保真补白。
5. 固定提示词为：

```text
去除右下角的gemini图标
```

6. 把该短句放进通用修图包装：

```text
You are an expert AI photo editor...

# EDIT INSTRUCTIONS:
去除右下角的gemini图标

# EDITING GUIDELINES:
1. Carefully perform the visual modification requested.
2. Ensure that edited regions blend naturally with surrounding colors, lighting, and textures.
3. Keep the rest of the image intact with high fidelity.
```

因此，Remove Watermark 不是 Custom 那种完全原生直发，但也不会使用双图 Fusion 商业摄影包装。

项目内虽然存在：

```text
utils/watermarkRemoval.ts
```

这个文件实现了浏览器本地像素修补算法，但当前没有被第一页导入或调用，不会影响实际结果。

---

## 七、非 Custom 的图片预处理与其他控制

### 7.1 图片压缩

非 Custom 模式中，超过 1.5MB 的图片会：

- 最长边缩小到不超过 1600px。
- 转换为 JPEG。
- JPEG 质量为 92%。

Custom 不执行此压缩。

### 7.2 高保真补白

非 Custom 模式选择新宽高比，并开启“高保真保留产品”时：

1. 浏览器先给原图增加白边，达到目标比例。
2. 服务端告诉模型图片已经补白。
3. 要求模型把白边扩展成连续背景。
4. 要求保留中央产品，并补充接触阴影和环境光。

单图时还会追加专门的高保真扩图提示词：

```text
- 保持中央产品完整。
- 不改变 Logo、纹理和产品文字。
- 扩展白色边缘区域。
- 生成接触阴影、反射和环境遮蔽。
- 消除原图与扩展区域的接缝。
```

Custom 不显示也不使用该模式。

### 7.3 固定比例但关闭高保真

非 Custom 模式选择固定宽高比，但关闭高保真时，会进入旧的“先描述、后重绘”流程：

1. 使用 `gemini-3.5-flash` 读取图片。
2. 把图片转换成主体、背景、光线和风格的文字描述。
3. 不再把原始图片直接交给最终图片模型。
4. 最终图片模型根据文字描述和用户要求重新生成。

这条流程更容易出现：

- 产品形状变化。
- Logo 或文字变化。
- 多图细节遗漏。
- 产品结构被重新想象。

---

## 八、最终发送关系总结

| 模式                | 可见提示词            | 隐藏提示词            | 图片预处理         |
| ----------------- | ---------------- | ---------------- | ------------- |
| Custom            | 用户原文或可见的 AI 优化结果 | 无                | 原图直发          |
| Remove Watermark  | 固定去水印短句          | 通用修图包装           | 只取第一张；可能压缩    |
| Background Fusion | 预置文字，可修改         | 通用包装 + 背景融合任务    | 可能压缩、补白或转文字描述 |
| Style & Lighting  | 预置文字，可修改         | 通用包装 + 风格光影任务    | 可能压缩、补白或转文字描述 |
| Replace Product   | 预置文字，可修改         | 通用包装 + 产品替换任务    | 可能压缩、补白或转文字描述 |
| Add Logo          | 预置文字，可修改         | 通用包装 + Logo 叠加任务 | 可能压缩、补白或转文字描述 |
| Replace Person    | 预置文字，可修改         | 通用包装 + 人物替换任务    | 可能压缩、补白或转文字描述 |

---

## 九、相关代码位置

以下行号可能会随着以后修改发生变化，应以函数名称为准。

- `App.tsx`
  - `PROMPT_TEMPLATES`：Fusion Mode 输入框预置内容。
  - `handleExpandPrompt`：AI 优化提示词按钮。
  - `handleGenerateClick`：调用第一页生成接口。
- `services/geminiService.ts`
  - `expandPromptForImage`：发送 AI 优化请求。
  - `optimizeImageForUpload`：非 Custom 图片压缩。
  - `padImageToAspectRatio`：高保真白边补齐。
  - `editImage`：根据模式决定最终图片、提示词和参数。
- `server.ts`
  - `optimizePromptForEditing`：通用修图提示词外层。
  - `buildDualImageSynchronizedPrompt`：双图 Fusion 隐藏提示词。
  - `buildSingleImageOutpaintingPrompt`：单图高保真扩图提示词。
  - `/api/expand-prompt`：AI 优化提示词接口。
  - `/api/edit-image`：最终图片编辑接口。
- `utils/watermarkRemoval.ts`
  - 未被实际调用的本地像素去水印算法。

---

## 十、维护提醒

以后如果修改提示词或处理流程，应同步更新本文档，重点检查：

1. Custom 是否仍然保持原生直发。
2. AI 优化结果是否仍然完整显示在输入框中。
3. Fusion Mode 是否仍然需要双层隐藏包装。
4. 多图规则是否还要求纳入全部产品素材。
5. 固定比例是否还存在“先描述、后重绘”流程。
6. 高保真模式是否仍然使用本地补白。
7. 去水印是否继续套用通用修图提示词。
8. 本地去水印文件是否开始被实际调用。

# 十一、原文

先说结论：第一页的 `Custom` 现在确实是原生直发，没有隐藏出图提示词；但整个 Fusion Mode 体系仍然存在较重的隐藏包装。因此“整个第一页都没有其他叠加”并不成立。

1. 当前“AI 优化提示词”的内部提示词

---------------------

它使用 `gemini-3.5-flash-lite`，只有你主动点击“✨ AI 优化提示词”才会调用。它不会自动运行。

核心系统提示词是：
    你是一个忠实、受控的电商图片提示词编辑器。你的任务是在绝不改变原意的前提下，整理用户原句，并且只对用户明确提出的要求维度做必要的执行性细化，使所选图像模型更容易准确执行。

    必须遵守：
    1. 用户原句是唯一事实来源。先识别用户明确提出了哪些维度，例如：任务目标、产品、文字内容、文字语言、文字排版、位置、颜色、数量、比例、场景、构图、风格、光线或需要保持不变的内容。
    2. 只能整理或细化用户已经明确提出的维度；没有提出的维度不得主动补充。原句已经明确时允许原样返回，绝不能为了体现“优化”而增加长度。
    3. 对用户已经提出但表达较笼统的维度，可以补充少量、不改变方向的执行性描述。例如“文字使用广告排版”可以细化为“主标题醒目、正文简短、字号层级清楚、对齐整齐、留白合理”，但这些描述只能约束文字排版。
    4. 必须逐项保留原句中每个要求的作用对象、主语、修饰对象、范围和限定关系，不得扩大、缩小、转移或偷换。
    5. 严格区分“文字内容”“文字语言”“文字排版”和“整体画面风格”：
       - 文字内容：用户提供的具体文字必须原样保留；除非用户明确要求，否则不得改写或翻译。
       - 文字语言：只规定最终图片中文字使用的语言，不得改成界面语言或画面风格。
       - 文字排版：只能细化标题层级、字号、字重、对齐、行距、位置、颜色、留白和可读性，不得扩大为整张画面的构图或风格。
       - 整体画面风格：只有用户明确要求广告图、海报、极简、奢华等画面风格时，才能细化场景、构图、光线和视觉氛围。
    6. 涉及数量、位置、颜色、比例，以及“只修改”“不要”“保持不变”等硬性要求时，必须保留原来的对象和约束。
    7. 不擅自增加商品、数量、品牌、具体文案、道具、人物、场景、风格、构图、教程、步骤、规格、价格、卖点或使用方法。用户要求文字排版但没有提供具体文案时，可以要求文字简短、层级清楚，并防止虚构价格、规格、品牌和卖点，但不得代写这些内容。
    8. 不主动提及上传图片的数量，不推断多张图片之间的关系，不要求所有图片内容全部出现；除非用户原句明确提出。
    9. 产品编辑时，可以在不冲突的情况下补充一句“保持产品外观和细节准确”。如果用户明确要求修改产品的某项属性，不得用保真要求阻止该修改。
    10. 不添加“世界顶级、杰作、8K、商业摄影大师”等空洞词语，不堆砌负面提示词。
    11. 输出应尽可能短，通常为1至3句，不设最低字数。只输出优化后的提示词正文，不要标题、解释、引号、Markdown或JSON。

还附带我们刚才制定的正确与错误示例。

同时会告诉文字模型当前选择的生图模型：

* Nano Banana：简短、直接，不增加多阶段任务。
* Nano Banana 2：准确保留对象、范围和限定关系。
* Nano Banana 2 Lite：保持单一任务，不扩展成复杂多阶段任务。
* Nano Banana Pro：可以表达复杂要求，但不主动增加构图、风格等维度。

代码位置：[server.ts (line 363)](D:\\Claude-Workspace\\ai-photo-editor\\server.ts:363)

2. Fusion Mode 用户看见的预置提示词

-------------------------

选择模式后，输入框自动写入：

| 场景      | 输入框预置内容                                               |
| ------- | ----------------------------------------------------- |
| Custom  | 空，不覆盖用户原文                                             |
| 去水印     | `去除右下角的gemini图标`                                      |
| 背景融合    | `请按参考图（图二）的背景场景与物理光影，将产品主图（图一）无缝融合，并在产品底部渲染真实物理接触阴影。` |
| 风格光影参考  | `请参考第二张图的光影氛围与美学色调，将产品主图置于其中，协调整体光照与环境反射。`            |
| 替换产品    | `请将参考场景（图二）中原有的旧物体移除，替换为图一中的主产品，严格匹配透视角度与阴影。`         |
| 叠加 Logo | `请将图二中的 LOGO/水印提取并自然叠加到产品主图之上，保持边缘清晰和适当透明度。`          |
| 替换人物    | `请保持图一中人物的动作和服装不变，将其面容与头部特征替换为图二中的目标人物，做到光影无缝融合。`     |

位置：[App.tsx (line 20)](D:\\Claude-Workspace\\ai-photo-editor\\App.tsx:20)

3. Fusion Mode 还有隐藏的服务端提示词

--------------------------

除 Custom 外，双图 Fusion 会额外定义图片角色：

* 前面的图片是主产品。
* 最后一张图片是参考场景、风格或光线蓝图。
* 强调产品保真、光线匹配、接触阴影、环境反射和避免贴纸感。
* 多张主图时，会写成“完整产品系列与套件”，并要求全部作为权威产品素材。

各模式还会加入不同任务规则：

* 背景融合：保留主产品形状、纹理、品牌、文字和材质反射，把它放进参考背景。
* 风格光影参考：保留主体结构和身份，融合参考图的氛围、光线、色调。
* 替换产品：删除参考图原产品，换成主图产品，匹配透视、景深和阴影。
* 叠加 Logo：把最后一张参考图当成 Logo，保持透明度和清晰边缘。
* 替换人物：替换人物身份，并匹配肤色反射、面部特征和脖颈阴影。

结尾还会追加：
    Generate a single, flawless, commercial-grade photograph...
    Ensure zero sticker effect...
    including all uploaded product assets!

然后这整个 Fusion 提示词还会再套进一层通用修图包装：
    You are an expert AI photo editor. Edit the provided image according to the following instructions.

    # EDIT INSTRUCTIONS:
    [完整的 Fusion 提示词]

    # EDITING GUIDELINES:
    1. Carefully perform the visual modification requested.
    2. Ensure that edited regions blend smoothly with surrounding colors, lighting, and textures.
    3. Keep the rest of the image intact with high fidelity.

所以 Fusion 实际上仍然是“双层包装”。位置：

* [server.ts (line 116)](D:\\Claude-Workspace\\ai-photo-editor\\server.ts:116)

* [server.ts (line 145)](D:\\Claude-Workspace\\ai-photo-editor\\server.ts:145)
4. Custom 现在有没有其他叠加？

--------------------

Custom 当前流程是：
    原始图片1
    原始图片2
    ……
    参考图片（如果有）
    输入框中当前可见的提示词

然后直接调用所选图片模型。

没有以下内容：

* 没有通用修图提示词。
* 没有商业摄影大师提示词。
* 没有图片角色包装。
* 没有强制所有图片都出现。
* 没有先让文字模型描述图片。
* 没有本地压缩、JPEG 转换或白边补齐。
* 没有高保真模式隐藏指令。
* 没有自动调用“AI 优化提示词”。

唯一可能加入的 API 控制是宽高比：
    {
      "imageConfig": {
        "aspectRatio": "16:9"
      }
    }

如果选择“保持原始比例”，连这个配置也不会发送。

位置：[server.ts (line 486)](D:\\Claude-Workspace\\ai-photo-editor\\server.ts:486)

5. 去水印不是完全原生

------------

去水印会强制：

* 只发送第一张图片。
* 忽略输入框改写，固定发送 `去除右下角的gemini图标`。
* 强制保持原比例。
* 关闭高保真补白。
* 再套一层上面的通用修图包装。

之前写过的本地像素去水印文件 [watermarkRemoval.ts (line 1)](D:\\Claude-Workspace\\ai-photo-editor\\utils\\watermarkRemoval.ts:1) 目前没有被任何页面引用，实际上不会运行。

6. 其他仍存在的控制

-----------

非 Custom 场景还存在：

* 大于 1.5MB 的图片会压缩到最长边最高 1600px，并转换为 92% JPEG。
* 高保真模式会先给图片补白，再要求模型扩展白边。
* 固定比例但关闭高保真时，会先用 `gemini-3.5-flash` 描述图片，再把文字描述交给生图模型重新生成；这条是旧的“先描述、后重绘”流程。
* Fusion 的多图角色和“全部产品素材”要求仍会影响结果。

所以最终准确结论是：

* `Custom`：已经纯净，只有当前可见提示词和宽高比参数。
* `AI 优化提示词`：只有点击后才运行，优化结果完全显示在输入框中。
* `去水印`：固定短句，但仍有一层通用修图包装。
* `其他 Fusion Mode`：仍然有较重的双层隐藏提示词和部分图片预处理。
* 第 2–5 页：仍是各自独立流程，没有随第一页一起修改。
