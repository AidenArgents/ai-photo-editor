# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.ai-photo-editor.pid
port3000.tmp
.runtime/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

backup_original

# Local maintainer-only release tools
/发布更新.bat
/GitHub发布与更新指南.md
/services/publish-update.ps1

# Release packaging artifacts (local only)
release.zip
version.txt
# 本地测试产物（订单汇总等），不进 git
out/
/services/publish-release.ps1
# 发布 Token（项目文件夹内，绝不提交）
.publish-token
# 注意：update-client.ps1 是客户端更新脚本，必须被 git 跟踪，不要加入此文件
